<Window x:Class="ColorClassifier.Window1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Test Classifiers" Height="600" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        
        <Button Grid.Row="0" Content="Train" Click="Button_Click"/>
        <Button Grid.Row="1" Content="Test" Click="Button_Click_1"/>
        <!--<ListView x:Name="ColorDisplay" Grid.Row="1">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" 
                                Background="{Binding}"
                                HorizontalAlignment="Stretch">
                        <ComboBox>
                            <ComboBoxItem>Red</ComboBoxItem>
                            <ComboBoxItem>Purple</ComboBoxItem>
                            <ComboBoxItem>Green</ComboBoxItem>
                            <ComboBoxItem>White</ComboBoxItem>
                            <ComboBoxItem>Other</ComboBoxItem>
                        </ComboBox>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Grid.Row="2">Train Knn</Button>-->
    </Grid>
</Window>
